# Local build artifacts (only appear if you compile inside the repo)
claude-lamp
claude-lamp-hooks
*.o
*.dSYM/

# Runtime / local state
state
term
settings.json.bak

# Raw screen recordings (ship a GIF/MP4 instead — see README)
*.mov
